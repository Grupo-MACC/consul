services:
  consul:
    image: consul:1.15
    container_name: consul-2
    hostname: consul-2
    ports:
      - "8500:8500"
      - "8600:8600/udp"
      - "8300:8300"
      - "8301:8301"
      - "8302:8302"
    extra_hosts:
      - "consul-1:10.1.11.40"
    environment:
      - CONSUL_BIND_INTERFACE=eth0
    volumes:
      - consul2_data:/consul/data
    command: >
      agent -server -ui
      -node=consul-2
      -bootstrap-expect=2
      -client=0.0.0.0
      -bind=0.0.0.0
      -advertise=10.1.11.31
      -retry-join=consul-1
      -datacenter=dc1
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "consul", "members"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  consul2_data:
services:
  consul:
    image: consul:1.15
    container_name: consul-1
    network_mode: host
    restart: unless-stopped

    volumes:
      - consul1_data:/consul/data

    command: >
      agent -server -ui
      -node=consul-1
      -bootstrap-expect=2
      -datacenter=dc1
      -bind=10.1.11.40
      -advertise=10.1.11.40
      -retry-join=10.1.11.41
      -client=0.0.0.0

    healthcheck:
      test: ["CMD", "consul", "members"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  consul1_data:
